---
- name: Symlink in config files
  file:
    src: "{{ playbook_dir }}/roles/user/files/{{ item }}"
    dest: "/home/{{ ansible_user }}/{{ item }}"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    state: link
    force: yes
  with_items:
    - .bash_aliases
    - .bashrc
    - .vimrc
    - .bash_profile
    - .gitconfig
    - .inputrc
    - .tmux.conf
    - .dockerrc
